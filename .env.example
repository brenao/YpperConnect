# Banco de dados (Lovable Cloud / Postgres)
# Preenchidos automaticamente no ambiente; não commitar valores reais.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# IA (endpoint compatível com OpenAI)
AI_BASE_URL=
AI_API_KEY=
AI_MODEL=gpt-4o-mini

# SMTP corporativo
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=YpperConnect <no-reply@rosset.com.br>
SMTP_REJECT_UNAUTHORIZED=true
